# OS_01 Exam

Welcome!

You have 60 minutes to complete the exam and submit the assessment form before the time runs out.

- 👉 For the duration of the exam **turn off your WiFi**.
- 👉 You only need this page open. **Close all other tabs/windows and programs.** Generative AI, chats, or other tools are **not permitted**.
- 👉 Some questions come with a code editor. You may **only** use it for that question. 
- 👉 Feel free to raise your hand to ask any questions! ✋

**When you're done**:

- Please click "Download My Answers" to download your work and enter your CODE email address.
- ✋ Raise your hand to indicate that you're done. 
- Now you may turn on your WiFi again. 
- Please **email the downloaded file** to [samuel.boguslawski@code.berlin](mailto:samuel.boguslawski@code.berlin)

_(👀 Please be aware that leaving this page or turning on the WiFi will show up in the logs.)_

Good luck, have fun!

---

## What is a function? (5 points)

Please describe what a function is and how it can be defined in Python. Please name at least one example for a concrete situation in which you would use a function.

---

## What is the difference between lists and dictionaries? When do you use which? (5 points)

Please describe what Python lists and dictionaries are and how they differ from each other. Please name at least one specific situation in which you would use lists and one for dictionaries.

---

## What will be printed in the command line after the following program is executed? (5 points)

It is probably a good idea to simulate the control flow of the code in your head and keep track of variables to find out which print statements are called in which order.

```python
a = "Ski-bi"
b = "dibby dib"
c = "da "
d = "dub "

x = a + b
y = "yo " + c + d * 2

print(x, y)

y = y.title()
print(y)
```

---

## What will be printed in the command line after the following program is executed? (5 points)

It is probably a good idea to simulate the control flow of the code in your head and keep track of variables and errors to find out which print statements are called in which order.

```python
try:
	you = 1
	life = 7
	print("Oh life")
	if life < "Universe":
		prin("is smaller")
	you = 1000
except Exception:
	print("is bigger")
	if life > you:
		print("is bigger than you")

try:
	me = int("one")
	if you == me:
		print("and you are me")
except Exception:
	print("and you are not me")
```

---

## What will be printed in the command line after the following program is executed? (5 points)

It is probably a good idea to simulate the control flow of the code in your head and keep track of variables to find out which print statements are called in which order.

```python
a = "ThIs AiNt No PlAcE FoR No "
b = ["hero" , "better man"]
c = "to call home"

a = a.lower()

for index in range(3):
	if index % 2 == 0:
		print(a + b[0])
	else:
		print(a + b[1])
		
		
print(c)
```

---

## Please define a function "age_filter(movies, max_age)" (10 points)

Given that a movie library is a list of dictionaries, each representing a movie with the attributes shown in the example below, please define a function `age_filter(movies, max_age)` that filters the list for all movies with age restriction under the maximum age. 

For example, calling `age_filter(movies, 17)` with the movie list from the image would filter out "Titanic" but not the "Gremlins" or "Home Alone". 

The editor already contains a dictionary as a starting point. You may alter it freely or use it to complete the task.

[[PYTHON-EDITOR]]
```python
movies = [  
  {  
    "name": "Titanic",  
    "actors": ["Leo", "Kate"],  
    "genre": "Horror",  
    "age_minimum": 18  
  },  
  {  
    "name": "Gremlins",  
    "actors": ["Gizmo"],  
    "genre": "Children",  
    "age_minimum": 16  
  },  
  {  
    "name": "Home Alone",  
    "actors": ["Kevin?"],  
    "genre": "Comedy",  
    "age_minimum": 12  
  }  
]

# Your code ...


```
[[/PYTHON-EDITOR]]

---

## Please define a function "input_list()" (10 points)

Please define a function that enables the user to input a list of strings via the command line. 

The function should ask the user repeatedly to input a string until the user inputs the string "END". It should then return the list of all strings the user has entered (not including the "END").

Examples: 

If the user inputs "Hello", "my", "name", "is", "Frodo" and "END", the function should return the list `["Hello","my","name","is","Frodo"]`.

[[PYTHON-EDITOR]]

---

## Please define a function count_big(nr_list) (10 points)

The function should take a list of numbers as input and count how many numbers are bigger than ten. It should return that count. 

**Example:** 

```
count_big([1, 17, 10, 14])

# Result: 2
```

[[PYTHON-EDITOR]]