/* ===================================================================
   index-page.js — Entry point for the home page
   =================================================================== */

import '../css/style.css';

/* Nothing dynamic needed for now — CSS import is enough to
   ensure the stylesheet is bundled by Vite. */
