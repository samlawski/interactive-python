# 1.3 Area of a Triangle (numbers and operations)

## Task

Please calculate and print the **area of a triangle**.

The first part of the code has already been given and should not be touched. It asks the user for the base and height of a triangle and stores them in the variables `b` and `h`.

Your job is to use these two variables to calculate the area and print it to the command line.


<details>
<summary>💡 Hints</summary>

- The given code asks the user to input `b` and `h`. We will learn how to do this next week.
- Please enter your solution after the comment `# your code below`, and don't change the other lines.
- If you get a `TypeError` in your print statement you likely tried to add together a string and a number. We'll learn how to do this next week. For now, you can just print the number and the string in separate print statements.
- Remember, the area of a triangle is calculated as: **A = ½ × b × h**

</details>
