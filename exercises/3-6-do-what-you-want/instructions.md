# 3.6 Do What You Want

## Task

Please write a program. **Any program.** Just think of something fun to do with the concepts you've learned.

Use comments to let us know **what** you did and **why**.

<details>
<summary>💡 Ideas</summary>

Not sure what to do? Here are a few suggestions:

- Write a function that greets a user by name and time of day
- Create a small math quiz that catches invalid answers
- Build a temperature converter using functions
- Write a function that checks whether a word is a palindrome
- Create a mini password strength checker

</details>

<details>
<summary>📚 What you've learned so far</summary>

- `print()` — to display text
- `input()` — to ask the user for something
- Variables — to store values
- Strings — text in quotes
- Numbers — integers and floats
- Operators — `+`, `-`, `*`, `/`, `**`, `%`
- String methods — `.upper()`, `.lower()`, `.strip()`, `.title()`
- Type conversion — `int()`, `float()`, `str()`
- Comparisons — `==`, `!=`, `<`, `>`, `<=`, `>=`
- Conditionals — `if`, `elif`, `else`
- Functions — `def`, parameters, `return`
- Error handling — `try`, `except`

</details>

Have fun! 🎨
