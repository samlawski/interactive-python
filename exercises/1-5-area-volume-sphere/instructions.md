# 1.5 Area and Volume of a Sphere (numbers and operations)

## Task

Please calculate and print the **surface area** and **volume** of a sphere.

The first part of the code has already been given and should not be touched. It asks the user for the radius of the sphere and stores it in the variable `r`. It also retrieves pi from a Python library and stores its value in the variable `pi`.

Your job is to calculate the surface area and volume of the sphere and print them to the command line.


<details>
<summary>💡 Hints</summary>

- The given code asks the user to input `r`. We will learn how to do this next week.
- Pi is taken from a module called `math`. We will learn how to work with modules later.
- Please enter your solution after the comment `# your code below`, and don't change the other lines.
- If you get a `TypeError` in your print statement, you likely tried to add together a string and a number. We'll learn how to do this next week. For now, you can just print the number and the string in separate print statements.
- Use `**` for exponentiation: `r ** 2` means r squared.

</details>
